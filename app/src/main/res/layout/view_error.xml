<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <ImageView
        android:id="@+id/error_ic"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:contentDescription="@string/error_connectivity"
        android:tint="@color/grey_500"
        app:layout_constraintBottom_toTopOf="@id/error_title"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintVertical_chainStyle="packed"
        app:srcCompat="@drawable/ic_error_36" />

    <TextView
        android:id="@+id/error_title"
        android:layout_width="@dimen/no_spacing"
        android:layout_height="wrap_content"
        android:layout_marginTop="@dimen/spacing_normal"
        android:fontFamily="@font/circularstd_book"
        android:gravity="center"
        android:text="@string/error_something_went_wrong"
        app:layout_constraintBottom_toTopOf="@id/error_msg"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/error_ic" />

    <TextView
        android:id="@+id/error_msg"
        android:layout_width="@dimen/no_spacing"
        android:layout_height="wrap_content"
        android:fontFamily="@font/circularstd_book"
        android:gravity="center"
        android:text="@string/error_connectivity"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/error_title" />

</androidx.constraintlayout.widget.ConstraintLayout>
